1. пишем класс модели llinear/logistic regression 
2. в классе должны быть методы
```
def __init__(...): 
#some code
def predict(...): 
#some code
def fit(...): 
#some code
```
и все остальное, что вам необходимо
3. берем датасет (для линейной - дома в калифорнии, для логистической - титаник)
4. предобрабатываем как можем, чтобы пропали object, остались float/bool/int
5. обучаем модель
6. выводим метрики качества (ошибки, точность предсказаний, etc.)
7. повторить для другого класса (т.е. если на шагах 1-6 делали линейную регрессию, тут надо сделать логистическу регрессию)

